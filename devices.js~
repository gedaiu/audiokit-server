var devices = {};

function init(server) {
	server.route({
	    method: 'GET',
	    path: '/devices',
	    handler: function (request, reply) {
		reply(JSON.stringify(devices));
	    }
	});
}


module.export = {
	init: init
};


